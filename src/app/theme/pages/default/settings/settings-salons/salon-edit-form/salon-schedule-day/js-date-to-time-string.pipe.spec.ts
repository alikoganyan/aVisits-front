import { JsDateToTimeStringPipe } from './js-date-to-time-string.pipe';

describe('JsDateToTimeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new JsDateToTimeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
